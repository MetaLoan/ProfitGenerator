# API 返回数据说明

## ✅ 完整的交易对信息

接口返回的 JSON 中，**顶层**就包含了所有交易对相关的信息，方便直接使用：

### 顶层字段结构

```json
{
  "success": true,
  "message": "ETH/USDT 做多 100x 杠杆，收益率 +243.79%",
  "tradepair": {
    "symbol": "ETHUSDT",
    "base": "ETH",
    "quote": "USDT",
    "display": "ETH/USDT"
  },
  "tradeInfo": {
    "opendate": "2025-12-03 04:23",
    "date": "2025-12-03 18:25",
    "direction": "做多",
    "leverage": 100,
    "entprice": 2996.38,
    "lastprice": 3069.43,
    "yield": "+243.79%"
  },
  "data": {
    "image": "data:image/png;base64,...",
    "base64": "...",
    "params": { ... }
  }
}
```

## 📋 字段说明

### 1. `message` - 文字描述
**可直接用于日志、提示信息**
```
"ETH/USDT 做多 100x 杠杆，收益率 +243.79%"
```

### 2. `tradepair` - 交易对详细信息
包含交易对的所有组成部分：
- `symbol`: 交易对符号（ETHUSDT）
- `base`: 基础货币（ETH）
- `quote`: 计价货币（USDT）
- `display`: 显示格式（ETH/USDT）

### 3. `tradeInfo` - 交易信息（顶层）
**所有图片中显示的数据都在这里：**
- `opendate`: 开仓时间
- `date`: 显示时间
- `direction`: 方向（做多/做空）
- `leverage`: 杠杆倍数
- `entprice`: 开仓价
- `lastprice`: 最新价
- `yield`: 收益率

### 4. `data.params` - 详细参数
包含所有生成参数和计算结果（与 tradeInfo 相同，但格式略有不同）

## 💡 使用示例

### JavaScript

```javascript
const response = await fetch('/api/generate?tradepair=ETHUSDT&...');
const result = await response.json();

// 直接获取文字描述
console.log(result.message);
// 输出: "ETH/USDT 做多 100x 杠杆，收益率 +243.79%"

// 获取交易对信息
console.log(result.tradepair.display); // "ETH/USDT"
console.log(result.tradepair.base);    // "ETH"

// 获取交易信息（图片中显示的所有数据）
console.log(result.tradeInfo.opendate);   // "2025-12-03 04:23"
console.log(result.tradeInfo.date);       // "2025-12-03 18:25"
console.log(result.tradeInfo.direction);  // "做多"
console.log(result.tradeInfo.leverage);   // 100
console.log(result.tradeInfo.entprice);   // 2996.38
console.log(result.tradeInfo.lastprice);  // 3069.43
console.log(result.tradeInfo.yield);      // "+243.79%"
```

### Python

```python
import requests

response = requests.get('http://localhost:3070/api/generate', params={
    'tradepair': 'ETHUSDT',
    'opendate': '2025-12-03 04:23',
    'date': '2025-12-03 18:25',
    'direction': 'long',
    'lev': 100
})

result = response.json()

# 文字描述
print(result['message'])

# 交易对信息
print(result['tradepair']['display'])

# 交易信息
print(result['tradeInfo'])
```

## 🎯 关键优势

1. **顶层即可访问**：所有信息都在 JSON 顶层，不需要深入 data.params
2. **完整信息**：包含图片中显示的所有数据
3. **多格式支持**：
   - `message` - 可直接显示的文字
   - `tradepair` - 交易对详细信息
   - `tradeInfo` - 结构化的交易数据
4. **易于使用**：字段名称清晰，直接对应图片内容

## 📊 数据对应关系

| 图片显示 | JSON 字段路径 |
|---------|--------------|
| 开仓时间 | `tradeInfo.opendate` |
| 显示时间 | `tradeInfo.date` |
| 做多/做空 | `tradeInfo.direction` |
| 100x | `tradeInfo.leverage` |
| 开仓价 | `tradeInfo.entprice` |
| 最新价 | `tradeInfo.lastprice` |
| 收益率 | `tradeInfo.yield` |
| 交易对 | `tradepair.display` |

所有图片中显示的数据都可以直接从 JSON 返回中获取！



