# API 返回数据结构说明

## ✅ 已实现的交易对信息

接口返回的 JSON 数据中，现在包含了完整的交易对相关信息：

### 1. 顶层字段

#### `message` - 文字描述信息
```
"message": "ETH/USDT 做多 50x 杠杆，收益率 +454.27%"
```
包含：
- 交易对显示格式（ETH/USDT）
- 交易方向（做多/做空）
- 杠杆倍数
- 收益率

#### `tradepair` - 交易对详细信息对象
```json
{
  "symbol": "ETHUSDT",
  "base": "ETH",
  "quote": "USDT",
  "display": "ETH/USDT"
}
```

### 2. data.params 字段

包含所有生成参数和计算结果：

```json
{
  "tradepair": "ETHUSDT",
  "tradepair_display": "ETH/USDT",
  "direction": "long",
  "direction_text": "做多",
  "lev": 50,
  "entprice": 2798.99,
  "lastprice": 3053.29,
  "yield": "+454.27%"
}
```

### 3. 错误响应

如果请求中包含了 `tradepair` 参数，错误响应中也会包含交易对信息：

```json
{
  "success": false,
  "error": "生成失败",
  "message": "ETH/USDT 图片生成失败: 浏览器上下文错误",
  "tradepair": {
    "symbol": "ETHUSDT",
    "base": "ETH",
    "quote": "USDT",
    "display": "ETH/USDT"
  }
}
```

## 📋 使用示例

### 获取交易对信息

```javascript
const response = await fetch('/api/generate?tradepair=ETHUSDT&...');
const result = await response.json();

if (result.success) {
  // 获取文字描述
  console.log(result.message);
  // 输出: "ETH/USDT 做多 50x 杠杆，收益率 +454.27%"
  
  // 获取交易对详细信息
  console.log(result.tradepair.display);
  // 输出: "ETH/USDT"
  
  console.log(result.tradepair.base);
  // 输出: "ETH"
  
  console.log(result.data.params.tradepair_display);
  // 输出: "ETH/USDT"
}
```

### 显示交易对信息

```javascript
// 在界面上显示
document.getElementById('tradeInfo').textContent = 
  `${result.tradepair.display} ${result.data.params.direction_text} ${result.data.params.lev}x`;

document.getElementById('yield').textContent = result.data.params.yield;
```

## 🔄 交易对格式说明

### 输入格式
- API 接受任意大小写：`ETHUSDT`、`ethusdt`、`EthUsdt`
- 统一转换为大写：`ETHUSDT`

### 输出格式
- `symbol`: 大写格式 `ETHUSDT`
- `display`: 显示格式 `ETH/USDT`
- `base`: 基础货币 `ETH`
- `quote`: 计价货币 `USDT`

## ✨ 优势

1. **信息完整**：返回数据中包含所有交易对相关信息
2. **易于使用**：提供多种格式，方便直接显示
3. **文字描述**：`message` 字段可直接用于日志或提示信息
4. **错误友好**：错误响应中也包含交易对信息，便于定位问题



